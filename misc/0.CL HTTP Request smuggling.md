So following the discussion from the portswigger youtube video on the 0.CL lab:

Things to try testing, in burpsuite pro the http request smuggler extension -> select on a target, suggestion is to make this request on many resources of an application, -> Parser discrepancy scan

- Once an interesting section is found test for it with sending a request on a static page, use GET if possible (make sure to turn off content length auto in burp repeater, and change method to http/1.1)
- Add your content-length to include only the first line to get chopped off by frontend
- sample here is to use `Content-Length : 1` with a space inbetween as this gets past to back end and will fail if you send a blank or X in the POST data request, or keep as GET I'm getting similar responses. May need to send multiple times to fool the cache delay
- Then add a new line under the first line in the request:
```
GET /resources/images/blog.svg HTTP/1.1
X: GET /resources/labheader/js/labheader.js HTTP/1.1
HOST: <host>
Cache-Control: max-age=0
Content-Type: application/x-www-form-urlencoded
Content-Length : 44

```
- This sends a request to the blog.svg for the frontend but is chopped off by the content length of 44 bytes, leaving the backend to see the request to the labheader.js. Will need to send multiple times to cause the descyn to happen and get your http request smuggled
- Now the next step for the burp lab is to get xss to trigger, info about there below:

## Portswigger getting common redirect to trigger xss in header

So James Kettle released research about this but essentially most applications have a behavior to redirect a user based on if they request a folder but without a trailing `/` if you add `?asdfa<>` this will be appended and not encoded allowing possible xss to happen with http request smuggling.

The way to abuse is find a resource with a returned content type of `text/html`